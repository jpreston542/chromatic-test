---
to: src/components/molecules/<%= name %>/<%= name %>.styles.js
---
import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
    root: {

    }
}));

export default useStyles;

